# 位运算示例

endian: big

# RGB 颜色提取
颜色 = 0xFF8040
R = (颜色 >> 16) & 0xFF
G = (颜色 >> 8) & 0xFF
B = 颜色 & 0xFF

# 查看位结构
bitmap 查看颜色 = 颜色

# IP 地址解析
IP = 0xC0A80001
段1 = (IP >> 24) & 0xFF
段2 = (IP >> 16) & 0xFF
段3 = (IP >> 8) & 0xFF
段4 = IP & 0xFF
